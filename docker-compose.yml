# ####################################
# USED ONLY FOR AUTOMATIC UNIT TESTS #
# ####################################

proxy:
  build: proxy

backend:
  build: backend

backendirods:
  build: backendirods

neo4j:
  build: neo4j

postgres:
  build: postgres

angular:
  build: angular

angular-dev:
  build: angular-dev

angular-test:
  build: angular-test

react:
  build: react

icat:
  build: icat

ftp:
  build: ftp

rabbitmq:
  build: rabbitmq

dind:
  build: dind
  privileged: true
