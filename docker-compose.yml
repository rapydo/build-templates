# ####################################
# USED ONLY FOR AUTOMATIC UNIT TESTS #
# ####################################

backend:
  build: backend

backendirods:
  build: backendirods

angular:
  build: angular

react:
  build: react

# celery:
#   build: celery

icat:
  # image: b2safe/server:icat
  build: icat

# b2safe:
#   # NOTE: this depends on icat
#   build: b2safe

restclient:
  build: restclient

ftp:
  build: ftp

proxy:
  build: proxy

dind:
  build: dind
  privileged: true

