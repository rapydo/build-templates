
# ####################################
# USED ONLY FOR AUTOMATIC UNIT TESTS #
# ####################################

backend:
  build: backend

celery:
  build: celery

icat:
  build: icat

restclient:
  build: restclient

swaggerui:
  build: swaggerui

ftp:
  build: ftp

proxy:
  build: proxy

sqlalchemyui:
  build: sqlalchemyui

